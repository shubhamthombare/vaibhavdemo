name: My Workflow

on:
  push:
    tags:
      - '*'

jobs:
  my-job:
    strategy:
      matrix:
        tag: [ '*' ]

    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      with:
        ref: ${{ matrix.tag }}
 
